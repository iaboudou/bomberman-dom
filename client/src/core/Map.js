export class Map {
  // initializes the map grid
  constructor(width, height) {
    this.width = width;
    this.height = height;
    this.grid = [];
  }

  // returns a Virtual DOM representation of the map
  render(el) { }
}
